CREATE TABLE thimira_foodItem_tab(
    item_id NUMBER GENERATED BY DEFAULT AS IDENTITY,
    item_name VARCHAR2(50) NOT NULL,
    available_qty NUMBER NOT NULL,
    scrapped_qty NUMBER NOT NULL,
    reserved_qty NUMBER NOT NULL,
    PRIMARY KEY(item_id)
);