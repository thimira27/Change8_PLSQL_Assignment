DECLARE
  -- variables
  -- constant
  -- cursors

BEGIN
  -- Invoke APIs

  -- create containerItems
  THIMIRA_CONTAINERITEMS_API.insert_data(5, 1, 'Stored');
  THIMIRA_CONTAINERITEMS_API.insert_data(6, 2, 'NotStored');
  THIMIRA_CONTAINERITEMS_API.insert_data(7, 3, 'NotStored');
  THIMIRA_CONTAINERITEMS_API.insert_data(8, 4, 'NotStored');
  THIMIRA_CONTAINERITEMS_API.insert_data(5, 4, 'Stored');

END;
/