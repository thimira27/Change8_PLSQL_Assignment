DECLARE
  -- variables
  -- constant
  -- cursors

BEGIN
  -- Invoke APIs

  -- create containerItems
  THIMIRA_CONTAINERITEMS_API.insert_data(1, 3, 'Stored');
  THIMIRA_CONTAINERITEMS_API.insert_data(2, 11, 'NotStored');
  THIMIRA_CONTAINERITEMS_API.insert_data(3, 4, 'NotStored');
  THIMIRA_CONTAINERITEMS_API.insert_data(4, 1, 'NotStored');
  THIMIRA_CONTAINERITEMS_API.insert_data(5, 2, 'stored');

END;
/